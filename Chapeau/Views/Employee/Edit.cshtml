@model Chapeau.Models.Employee

<h2>Edit Employee</h2>

<form method="post" action="@Url.Action("Edit")">
    <input type="hidden" name="EmployeeID" value="@Model.EmployeeID" />

    <div class="form-group mb-3">
        <label>Name</label>
        <input type="text" name="Name" value="@Model.Name" class="form-control" required />
    </div>
    <div class="form-group mb-3">
        <label>Role</label>
        <select name="Role" class="form-control" required>
            @if (Model.Role == "Manager") { <option value="Manager" selected>Manager</option> } else { <option value="Manager">Manager</option> }
            @if (Model.Role == "Waiter") { <option value="Waiter" selected>Waiter</option> } else { <option value="Waiter">Waiter</option> }
            @if (Model.Role == "Chef") { <option value="Chef" selected>Chef</option> } else { <option value="Chef">Chef</option> }
            @if (Model.Role == "Bartender") { <option value="Bartender" selected>Bartender</option> } else { <option value="Bartender">Bartender</option> }
        </select>
    </div>
    <div class="form-group mb-3">
        <label>Username / Employee Number</label>
        <input type="text" name="Username" value="@Model.Username" class="form-control" required />
    </div>
    <div class="form-group mb-3">
        <label>Password Hash / PIN</label>
        <input type="text" name="PasswordHash" value="@Model.PasswordHash" class="form-control" required />
    </div>
    <div class="form-group mb-3">
        <label>
            <input type="checkbox" name="IsActive" value="true" @(Model.IsActive ? "checked" : "") /> Is Active
        </label>
    </div>

    <button type="submit" class="btn btn-primary">Update</button>
    <a href="@Url.Action("Index")" class="btn btn-secondary">Cancel</a>
</form>
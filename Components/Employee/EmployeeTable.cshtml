@model Chapeau.Components.EmployeeTable.EmployeeTableModel

<link rel="stylesheet" href="~/css/components/employee/employee-table.css" />

<div class="employee-table-card">
    <table class="employee-table">
        <thead>
            <tr>
                <th>Naam</th>
                <th>Gebruikersnaam</th>
                <th>Rol</th>
                <th>Status</th>
                <th>Acties</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var employee in Model.Employees)
            {
                var usernameBase = (employee.Name ?? "")
                    .Split(' ', StringSplitOptions.RemoveEmptyEntries)
                    .FirstOrDefault()?.ToLowerInvariant() ?? "user";

                string username = $"{usernameBase}{employee.EmployeeID:000}";

                string roleName = Model.RoleNameById.TryGetValue(employee.RoleID, out var rn)
                    ? rn ?? employee.RoleID.ToString()
                    : employee.RoleID.ToString();

                string rowClass = employee.IsActive ? "" : "inactive-row";

                <tr class="@rowClass">
                    <td>@employee.Name</td>
                    <td>@username</td>
                    <td>@roleName</td>

                    <td>
                        @if (employee.IsActive)
                        {
                            <span class="status-badge status-active">Actief</span>
                        }
                        else
                        {
                            <span class="status-badge status-inactive">Inactief</span>
                        }
                    </td>

                    <td>
                        <div class="action-cell">
                            <a href="@Url.Action("Index", "Employee", new { editId = employee.EmployeeID })"
                                   class="action-icon action-edit preserve-scroll"
                                   title="Bewerken">
                                <svg xmlns="http://www.w3.org/2000/svg"
                                     fill="none"
                                     viewBox="0 0 24 24"
                                     stroke="currentColor">
                                    <path stroke-linecap="round"
                                          stroke-linejoin="round"
                                          d="M16.862 4.487l1.687-1.688a1.875 1.875 0 112.652 2.652L10.582 16.07a4.5 4.5 0 01-1.897 1.13L6 18l.8-2.685a4.5 4.5 0 011.13-1.897l8.932-8.931z" />
                                    <path stroke-linecap="round"
                                          stroke-linejoin="round"
                                          d="M19.5 7.125L16.875 4.5" />
                                    <path stroke-linecap="round"
                                          stroke-linejoin="round"
                                          d="M18 14v4.75A2.25 2.25 0 0115.75 21H5.25A2.25 2.25 0 013 18.75V8.25A2.25 2.25 0 015.25 6H10" />
                                </svg>
                            </a>

                            <form method="post"
                                  action="@Url.Action("ToggleActive", "Employee")"
                                  class="inline-action-form preserve-scroll-form">

                                @Html.AntiForgeryToken()

                                <input type="hidden" name="id" value="@employee.EmployeeID" />

                                @if (employee.IsActive)
                                {
                                    <input type="hidden" name="active" value="false" />

                                    <button type="submit"
                                            class="action-icon action-inactive preserve-scroll"
                                            title="Deactiveren">
                                        <svg xmlns="http://www.w3.org/2000/svg"
                                             fill="none"
                                             viewBox="0 0 24 24"
                                             stroke="currentColor">
                                            <path stroke-linecap="round"
                                                  stroke-linejoin="round"
                                                  d="M12 3v9" />
                                            <path stroke-linecap="round"
                                                  stroke-linejoin="round"
                                                  d="M6.75 6.75a7.5 7.5 0 1010.5 0" />
                                            <path stroke-linecap="round"
                                                  stroke-linejoin="round"
                                                  d="M4 4l16 16" />
                                        </svg>
                                    </button>
                                }
                                else
                                {
                                    <input type="hidden" name="active" value="true" />

                                    <button type="submit"
                                            class="action-icon action-active preserve-scroll"
                                            title="Activeren">
                                        <svg xmlns="http://www.w3.org/2000/svg"
                                             fill="none"
                                             viewBox="0 0 24 24"
                                             stroke="currentColor">
                                            <path stroke-linecap="round"
                                                  stroke-linejoin="round"
                                                  d="M12 3v9" />
                                            <path stroke-linecap="round"
                                                  stroke-linejoin="round"
                                                  d="M6.75 6.75a7.5 7.5 0 1010.5 0" />
                                        </svg>
                                    </button>
                                }
                            </form>
                        </div>
                    </td>
                </tr>

                @if (Model.EditEmployee != null && Model.EditEmployee.EmployeeID == employee.EmployeeID)
                {
                    <tr class="inline-edit-row">
                        <td colspan="5">
                            <div class="inline-edit-card">
                                <h3>Medewerker Bewerken</h3>

                                <form method="post"
                                      action="@Url.Action("Edit", "Employee")"
                                      class="employee-form preserve-scroll-form">

                                    @Html.AntiForgeryToken()

                                    <input type="hidden" name="EmployeeID" value="@Model.EditEmployee.EmployeeID" />
                                    <input type="hidden" name="IsActive" value="@Model.EditEmployee.IsActive.ToString().ToLower()" />

                                    <div class="form-group">
                                        <input type="text"
                                               name="Name"
                                               class="form-control"
                                               placeholder="Naam"
                                               value="@Model.EditEmployee.Name"
                                               required="required" />
                                    </div>

                                    <div class="form-group">
                                        <input type="password"
                                               name="PasswordHash"
                                               class="form-control"
                                               placeholder="Nieuw wachtwoord optioneel" />
                                    </div>

                                    <div class="form-group">
                                        <select name="RoleID" class="form-select" required="required">
                                            <option value="">-- Selecteer rol --</option>

                                            @foreach (var role in (ViewBag.Roles as IEnumerable<Chapeau.Models.EmployeeRole>) ?? [])
                                            {
                                                if (Model.EditEmployee.RoleID == role.RoleID)
                                                {
                                                    <option value="@role.RoleID" selected="selected">@role.RoleName</option>
                                                }
                                                else
                                                {
                                                    <option value="@role.RoleID">@role.RoleName</option>
                                                }
                                            }
                                        </select>
                                    </div>

                                    <div class="form-actions">
                                        <button type="submit" class="btn btn-primary preserve-scroll">
                                            Bijwerken
                                        </button>

                                        <a href="@Url.Action("Index", "Employee")"
                                               class="btn btn-secondary preserve-scroll">
                                            Annuleren
                                        </a>
                                    </div>
                                </form>
                            </div>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>
